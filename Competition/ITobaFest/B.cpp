#include <bits/stdc++.h>

using namespace std;

void printMatrix(string kata)
{
    int len = kata.size();
    int len2 = len/2-1;
    int mlen2 = -(len2);

    for()
}

int main(){
    int n;
    string kata;

    cin>>n;

    while(n--){
        cin>>kata;
        printMatrix(kata);
    }

    return 0;
}
