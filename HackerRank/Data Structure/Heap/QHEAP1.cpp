#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, op, bil;
    vector<int> data;

    cin>>n;

    while(n--){
        cin>>op;
        if(op == 1){
            cin>>bil;
            data.push_back(bil);
        }else if(op == 2){
            cin>>bil;

        }
    }

    return 0;
}
